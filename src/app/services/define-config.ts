import type {NgPressConfig} from '../types';

/**
 * Define the configuration for the application.
 * @param config
 */
export default function defineConfig(config: NgPressConfig): NgPressConfig {
  return config;
}
